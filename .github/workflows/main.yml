name: Build Minimal Linux x86

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-os:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install Build Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y bc bison flex libelf-dev libssl-dev libncurses-dev cpio xorriso syslinux syslinux-utils isolinux

    - name: Set up Workspace Directories
      run: |
        mkdir -p workspace/rootfs/{bin,sbin,etc,proc,sys,dev,usr/bin,usr/sbin}
        mkdir -p workspace/iso/boot/isolinux

    - name: Download Sources (Kernel & BusyBox)
      run: |
        cd workspace
        # Download Stable Linux Kernel (Contoh: v6.1)
        curl -L https://kernel.org | tar -xJ
        mv linux-6.1.50 linux-src
        # Download BusyBox
        curl -L https://busybox.net | tar -xj
        mv busybox-1.36.1 busybox-src

    - name: Compile Linux Kernel
      run: |
        cd workspace/linux-src
        make x86_64_defconfig
        # Optimasi ukuran (opsional, bisa diganti tinyconfig)
        scripts/config --disable DEBUG_INFO
        make -j$(nproc) bzImage
        cp arch/x86/boot/bzImage ../iso/boot/vmlinuz

    - name: Compile BusyBox (Static Binary)
      run: |
        cd workspace/busybox-src
        make defconfig
        # Paksa busybox dicompile secara statis tanpa shared library
        sed -i 's/# CONFIG_STATIC is not set/CONFIG_STATIC=y/' .config
        make -j$(nproc)
        make CONFIG_PREFIX=../rootfs install

    - name: Create Init Script & Build Initramfs
      run: |
        cd workspace
        # Buat script init
        cat << 'EOF' > rootfs/init
        #!/bin/sh
        mount -t proc none /proc
        mount -t sysfs none /sys
        mount -t devtmpfs none /dev
        echo "========================================"
        echo " Selamat datang di OS Linux Minimal Anda! "
        echo "========================================"
        exec /bin/sh
        EOF
        chmod +x rootfs/init
        
        # Packing menjadi initramfs.cpio.gz
        cd rootfs
        find . -print0 | cpio --null -ov --format=newc | gzip -9 > ../iso/boot/initramfs.cpio.gz

    - name: Configure Bootloader (Isolinux)
      run: |
        cd workspace/iso/boot/isolinux
        # Salin file bootloader dari sistem Ubuntu Runner
        cp /usr/lib/ISOLINUX/isolinux.bin .
        cp /usr/lib/syslinux/modules/bios/ldlinux.c32 .
        
        # Buat konfigurasi isolinux
        cat << 'EOF' > isolinux.cfg
        default minimal
        label minimal
          kernel /boot/vmlinuz
          append initrd=/boot/initramfs.cpio.gz console=ttyS0 console=tty0
        EOF

    - name: Create ISO Image
      run: |
        cd workspace
        xorriso -as mkisofs -o minimal_linux.iso \
          -b boot/isolinux/isolinux.bin \
          -c boot/isolinux/boot.cat \
          -no-emul-boot -boot-load-size 4 -boot-info-table \
          iso

    - name: Upload ISO Artifact
      uses: actions/upload-artifact@v4
      with:
        name: minimal-linux-iso
        path: workspace/minimal_linux.iso
